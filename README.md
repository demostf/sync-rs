# Moved to https://codeberg.org/demostf/sync

# Sync

Websocket api to sync playback of demos

By default, the websocket server listens on port 80, this can be changed by settings the PORT environment variable.
